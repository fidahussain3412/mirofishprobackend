
# MiroFish Backend

## How to Run

To run the project, use the following command:

```bash
python run.py
```

This will start the Flask development server.

## Base URL

The base URL for the backend service is:

```
http://localhost:5001/
```

The port can be configured via the environment variable `FLASK_PORT` (default is 5001).


